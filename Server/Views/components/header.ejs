<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <img class="navbar-brand" src="/Assets/images/logo.png" href="/">Min Li</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
  
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
  
          <li class="nav-item">
          <% if (title == "Home") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
             href="/"><i class="fas fa-lg fa-laptop-house"></i>Home</a>
          </li>
  
          <li class="nav-item">
          <% if (title == "About") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
             href="/about"><i class="fas fa-lg fa-info-circle"></i>About</a>
          </li>
  
          <li class="nav-item">
          <% if (title == "Products") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
            href="/projects"><i class="fab fa-lg fa-elementor"></i>Projects</a>
          </li>
  
          <li class="nav-item">
          <% if (title == "Services") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
            href="/services"><i class="fas fa-lg fa-cogs"></i>Services</a>
          </li>
          
          <li class="nav-item">
          <% if (title == "Contact Us") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
            href="/contact"><i class="fas fa-lg fa-cogs"></i>Contact Us</a>
          </li>
  
      </div>
    </div>
  </nav>